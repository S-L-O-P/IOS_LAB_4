#set page(paper: "a4", margin: (x: 2cm, y: 1.5cm))
#set text(size: 14pt)
#set text(lang: "ru", hyphenate: true)

#let report-image(path, caption: none, width: 100%) = {
  let body = image(path, width: width)
  if caption == none {
    align(center, body)
  } else {
    figure(body, caption: [#caption])
  }
}

#align(center, [
  #text(size: 14pt, "Министерство образования и науки Российской Федерации") \
  #text(size: 14pt, "Федеральное Государственное Автономное Образовательное") \
  #text(size: 14pt, "Учреждение Высшего Образования \"Национальный") \
  #text(size: 14pt, "Исследовательский Университет ИТМО\"")

  #v(2cm)

  #text(size: 14pt, "Факультет программной инженерии и компьютерной техники") \
  #text(size: 14pt, "Дисциплина \"Системы ввода-вывода\"")

  #v(4cm)

  #text(size: 16pt, weight: "bold", "Лабораторная работа №4") \
  #text(size: 14pt, "Изучение работы контроллеров
ввода/вывода") \
])

#v(3.725cm)

#align(right, [
  Выполнили \
  Джозеф Байден \
  Касым Жомарт-Токаев \
  Си Цзиньпинь \
  Группа P3319
])

#v(1cm)

#align(right, [
  Проверил \
  Владимир Путин
])

#v(3.75cm)

#align(center, [
  г. Санкт-Петербург, 2026 г
])

#pagebreak()

#set heading(numbering: "1.")

= Введение

*Цель работы:* познакомиться с принципами работы контроллеров ввода/вывода на примере контроллера UART микроконтроллера ATmega328.

*Задачи:*
+ Реализовать драйвер UART на уровне регистров аппаратного контроллера ATmega328 (вариант~2: датчик BMP280 по SPI, 38400~бод, odd parity, 2 стоп-бита).
+ Реализовать приём пакетов с проверкой целостности (CRC8) и отправкой эхо-ответа.
+ Реализовать периодическую передачу данных датчика BMP280 (температура и давление) раз в секунду.
+ Написать клиентскую программу на Python для тестирования корректных и некорректных пакетов.
+ Подключить микроконтроллер к ПК и протестировать работу.
+ Снять осциллограмму передачи пакета по интерфейсу UART.

= Формат пакета

Пакет состоит из четырёх полей, передаваемых последовательно:

#table(
  columns: (auto, auto, 1fr),
  stroke: 0.5pt,
  [*Поле*], [*Размер*], [*Описание*],
  [S], [1 байт], [Синхробайт 0x5A -- маркер начала пакета],
  [LEN], [1 байт], [Количество байт в поле DATA (0–255)],
  [DATA], [LEN байт], [Полезная нагрузка],
  [CRC8], [1 байт], [Контрольная сумма (полином 0x07, init 0x00), считается по полям LEN + DATA],
)

Пакет с данными датчика содержит 8 байт: `float` температура (°C) + `float` давление (Па), little-endian.

= Реализация

Полный исходный код: `uart/uart.ino` (микроконтроллер), `client/client.py` (клиент на ПК)

== Программа микроконтроллера (`uart/uart.ino`)

*Инициализация UART.* Скорость задаётся через делитель UBRR, формат кадра -- через биты регистров UCSR0B и UCSR0C:

```cpp
// Скорость 38400 бод: UBRR = f_osc/(16*BAUD)-1 = 25 (ATmega328P datasheet, стр. 162)
uint16_t ubrr = 16000000 / 16 / 38400 - 1;
UBRR0H = (unsigned char)(ubrr >> 8);
UBRR0L = (unsigned char)ubrr;

SetBit(UCSR0B, TXEN0);   // разрешить передатчик
SetBit(UCSR0B, RXEN0);   // разрешить приёмник
SetBit(UCSR0B, RXCIE0);  // прерывание по завершению приёма байта

SetBit(UCSR0C, UCSZ01);  // \ 8 бит данных (UCSZ1:0=11, Table 19-7)
SetBit(UCSR0C, UCSZ00);  // /
SetBit(UCSR0C, UPM01);   // \ odd parity (UPM1:0=11, Table 19-5)
SetBit(UCSR0C, UPM00);   // /
SetBit(UCSR0C, USBS0);   // 2 стоп-бита (USBS=1, Table 19-6)
```

*Отправка байта* -- ожидание флага UDRE0 (буфер свободен), затем запись в UDR0:

```cpp
void uartSendByte(uint8_t b) {
  while (!(UCSR0A & (1 << UDRE0)));  // ждём освобождения буфера TX (стр. 159)
  UDR0 = b;                          // запись запускает передачу
}
```

*Приём -- прерывание `ISR(USART_RX_vect)`.* Каждый принятый байт обрабатывается конечным автоматом с четырьмя состояниями (S\_SYNC -> S\_LEN -> S\_DATA -> S\_CRC). При совпадении CRC пакет помечается готовым к эхо-ответу; битый пакет молча отбрасывается, автомат возвращается в S\_SYNC.

*Данные датчика* -- раз в секунду `loop()` вызывает `sendSensorPacket()`, которая читает температуру и давление с BMP280 по SPI и отправляет их как два `float` (8 байт, little-endian) в штатном формате пакета.

== Клиентская программа (`client/client.py`)

*Открытие порта* с параметрами варианта 2:

```python
ser = serial.Serial(
    PORT, 38400,
    bytesize=serial.EIGHTBITS,
    parity=serial.PARITY_ODD,
    stopbits=serial.STOPBITS_TWO,
    timeout=2,
)
```

*CRC8* (полином 0x07, init 0x00) -- идентичен реализации на МК; считается по полям LEN + DATA:

```python
def crc8(data):
    crc = 0x00
    for byte in data:
        crc ^= byte
        for _ in range(8):
            crc = ((crc << 1) ^ 0x07) & 0xFF if (crc & 0x80) else (crc << 1) & 0xFF
    return crc
```

*Тесты* -- функция `run_tests()` последовательно отправляет пять вариантов и слушает эхо в течение 1.5 с; корректный пакет должен вернуться, остальные -- нет. Данные датчика читаются в `main()` через `read_packet()`: 8 байт распаковываются как `struct.unpack("<ff", data)` -> температура и давление.

= Тестирование

Клиентская программа последовательно отправляет пять тестовых случаев и проверяет, вернул ли микроконтроллер эхо:

#table(
  columns: (1.8fr, 2fr, auto),
  stroke: 0.5pt,
  [*Тест*], [*Описание ошибки*], [*Результат*],
  [Корректный пакет], [Пакет без ошибок], [ПРИНЯТ],
  [Нет синхробайта], [Первый байт 0x5A пропущен], [ОТБРОШЕН],
  [Неправильная длина], [LEN=2, но данных 4 байта], [ОТБРОШЕН],
  [Недостаточно данных], [LEN=8, передано только 2 байта], [ОТБРОШЕН],
  [Битый CRC], [Последний байт инвертирован (XOR 0xFF)], [ОТБРОШЕН],
)

Микроконтроллер принимает только первый тест и игнорирует остальные. В непрерывном режиме контроллер раз в секунду отправляет пакет с данными датчика BMP280: температурой (°C) и давлением (Па) в формате двух `float` little-endian.

= Осциллограмма

// TODO: вставить скриншот из Logic 2 с временной диаграммой передачи пакета

